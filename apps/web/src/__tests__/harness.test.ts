import { describe, expect, it } from 'vitest'

// Ponytail: trivial sanity check that the harness boots. Replace with real
// tests once the suite grows.
describe('vitest harness', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2)
  })
})
