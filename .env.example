# CFA — Environment Variables Reference
# Copy to .env and fill in your values
# See apps/api/.env.example and apps/web/.env.example for app-specific vars

# === DATABASE (shared across all services) ===
DATABASE_URL="postgresql://postgres:password@localhost:5432/cfa_dev"
