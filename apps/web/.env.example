# API URL — used by the API client
# In development, Vite proxies /api to this URL
VITE_API_URL=http://localhost:3001

# App environment
VITE_APP_ENV=development

# Auth redirect URL — used for email verification and password reset callbacks
VITE_AUTH_REDIRECT_URL=http://localhost:5174/auth
